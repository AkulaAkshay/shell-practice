#!/bin/bash

#we did not pass the variable name as read command, we din't pass the variables here also, we din't pass the var even as arguments also
# we will pass the variable in the ~/.bashrh file 
echo "Course name is: $COURSE"