#!/bin/bash -- she bang is used to tell the shell that this script is written in bash shell, so use bash shell to execute this script.

#--> this is comment, can't be executed, oonly for the information purpose [except "#" in the 1st line]

echo "Hello world!"